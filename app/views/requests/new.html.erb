


<%= form_for @request do |f| %>
<%= f.label :dates %> 
<%= f.text_field :dates %> 
<%= f.label :content %>
<%= f.text_area :content %>
<%= f.hidden_field :requestee_padder_id, :value => :requestee_padder_id%>
<%= f.hidden_field :requester_padder_id, :value => @current_padder.id %>
<%= f.submit %>
<% end %>
  